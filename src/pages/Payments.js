import React from 'react';
import BackButton from "../components/BackButton";

const Payments = () => {
    return (
        <div className="page">
            <BackButton/>
            <h1>Вкладка Оплаты</h1>
            <p>История и управление платежами.</p>
        </div>
    );
};

export default Payments;